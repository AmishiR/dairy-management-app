| sql                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CREATE POLICY audit_logs_staff_only ON public.audit_logs AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING (is_staff_or_admin());                                                                                                                                                                        |
| CREATE POLICY customer_prices_select_own_or_staff ON public.customer_prices AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING (((customer_id = current_customer_id()) OR is_staff_or_admin()));                                                                                                          |
| CREATE POLICY customer_prices_staff_manage ON public.customer_prices AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                              |
| CREATE POLICY customers_select_own_or_staff ON public.customers AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING (((user_id = auth.uid()) OR is_staff_or_admin()));                                                                                                                                     |
| CREATE POLICY customers_staff_manage ON public.customers AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                          |
| CREATE POLICY deliveries_select_own_or_staff ON public.deliveries AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING (((customer_id = current_customer_id()) OR is_staff_or_admin()));                                                                                                                    |
| CREATE POLICY deliveries_staff_manage ON public.deliveries AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                        |
| CREATE POLICY delivery_items_select_own_or_staff ON public.delivery_items AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING ((is_staff_or_admin() OR (EXISTS ( SELECT 1
   FROM deliveries d
  WHERE ((d.delivery_id = delivery_items.delivery_id) AND (d.customer_id = current_customer_id()))))));     |
| CREATE POLICY delivery_items_staff_manage ON public.delivery_items AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                |
| CREATE POLICY internal_costs_staff_only ON public.internal_costs AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                  |
| CREATE POLICY inventory_balances_staff_only ON public.inventory_balances AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                          |
| CREATE POLICY inventory_txn_staff_only ON public.inventory_transactions AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                           |
| CREATE POLICY order_items_insert_own ON public.order_items AS PERMISSIVE FOR INSERT TO "unknown (OID=0)" WITH CHECK ((EXISTS ( SELECT 1
   FROM orders o
  WHERE ((o.order_id = order_items.order_id) AND (o.customer_id = current_customer_id())))));                                                     |
| CREATE POLICY order_items_select_own_or_staff ON public.order_items AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING ((is_staff_or_admin() OR (EXISTS ( SELECT 1
   FROM orders o
  WHERE ((o.order_id = order_items.order_id) AND (o.customer_id = current_customer_id()))))));                        |
| CREATE POLICY order_items_staff_manage ON public.order_items AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                      |
| CREATE POLICY orders_insert_own ON public.orders AS PERMISSIVE FOR INSERT TO "unknown (OID=0)" WITH CHECK ((customer_id = current_customer_id()));                                                                                                                                                         |
| CREATE POLICY orders_select_own_or_staff ON public.orders AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING (((customer_id = current_customer_id()) OR is_staff_or_admin()));                                                                                                                            |
| CREATE POLICY orders_staff_manage ON public.orders AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                                |
| CREATE POLICY payment_verifications_select ON public.payment_verifications AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING ((is_staff_or_admin() OR (EXISTS ( SELECT 1
   FROM payments p
  WHERE ((p.payment_id = payment_verifications.payment_id) AND (p.customer_id = current_customer_id())))))); |
| CREATE POLICY payments_insert_own_or_staff ON public.payments AS PERMISSIVE FOR INSERT TO "unknown (OID=0)" WITH CHECK (((customer_id = current_customer_id()) OR is_staff_or_admin()));                                                                                                                   |
| CREATE POLICY payments_select_own_or_staff ON public.payments AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING (((customer_id = current_customer_id()) OR is_staff_or_admin()));                                                                                                                        |
| CREATE POLICY payments_staff_update ON public.payments AS PERMISSIVE FOR UPDATE TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                         |
| CREATE POLICY production_batches_staff_only ON public.production_batches AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                          |
| CREATE POLICY production_inputs_staff_only ON public.production_inputs AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                            |
| CREATE POLICY production_recipe_items_staff_only ON public.production_recipe_items AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                |
| CREATE POLICY production_recipes_staff_only ON public.production_recipes AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                          |
| CREATE POLICY products_select_authenticated ON public.products AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING ((auth.uid() IS NOT NULL));                                                                                                                                                             |
| CREATE POLICY products_staff_manage ON public.products AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                                            |
| CREATE POLICY profiles_admin_manage ON public.profiles AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_admin()) WITH CHECK (is_admin());                                                                                                                                                              |
| CREATE POLICY profiles_select_own_or_staff ON public.profiles AS PERMISSIVE FOR SELECT TO "unknown (OID=0)" USING (((id = auth.uid()) OR is_staff_or_admin()));                                                                                                                                            |
| CREATE POLICY profiles_update_own ON public.profiles AS PERMISSIVE FOR UPDATE TO "unknown (OID=0)" USING ((id = auth.uid()));                                                                                                                                                                              |
| CREATE POLICY raw_receipt_items_staff_only ON public.raw_material_receipt_items AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                   |
| CREATE POLICY raw_receipts_staff_only ON public.raw_material_receipts AS PERMISSIVE FOR ALL TO "unknown (OID=0)" USING (is_staff_or_admin()) WITH CHECK (is_staff_or_admin());                                                                                                                             |